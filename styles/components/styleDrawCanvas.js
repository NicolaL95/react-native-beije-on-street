import { StyleSheet } from "react-native";

export default StyleSheet.create({
    canvasBlock: {
        position: 'absolute',
        top: 0,
        zIndex: 9,
        backgroundColor: 'white',
        width: '100%',
        height: '100%',
        opacity: 0.1
    }
})